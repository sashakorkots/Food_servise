@page
@model Food_servise.Pages_Regions.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Region</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Region.Id" />
            <div class="form-group">
                <label asp-for="Region.RestrantId" class="control-label">Restaurant</label>
                <select asp-for="Region.RestrantId" class="form-control" asp-items="ViewBag.RestrantId"></select>
                <span asp-validation-for="Region.RestrantId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Region.CourierId" class="control-label">Courier</label>
                <select asp-for="Region.CourierId" class="form-control" asp-items="ViewBag.CourierId"></select>
                <span asp-validation-for="Region.CourierId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
