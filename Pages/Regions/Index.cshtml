@page
@model Food_servise.Pages_Regions.IndexModel

@{
    ViewData["Title"] = "Regions";
}

<h1>Regions</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Restaurant
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Region[0].CourierOfRegion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Region) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RestrantOfRegion.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CourierOfRegion.Name)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
